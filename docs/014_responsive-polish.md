# 014: レスポンシブ対応・UI仕上げ

## 概要

全ページのレスポンシブ対応と、UI全体の仕上げを行う。

## Todo

- [x] 全ページのモバイル表示確認・調整
- [x] 動画視聴ページのプレイヤーサイズ調整（モバイル対応）
- [x] `error.tsx`（ルート）の作成（エラーページ）
- [x] `not-found.tsx`（ルート）の作成（404ページ）
- [x] メタデータの設定（各ページの title, description）
- [x] `npm run build` でビルドエラーがないことを確認
- [x] `npm run lint` でリントエラーがないことを確認
