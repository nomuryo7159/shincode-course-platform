# 001: プロジェクト初期セットアップ

## 概要

Next.js 16 プロジェクトに必要な追加パッケージのインストールと基盤設定を行う。

## Todo

- [x] Supabase パッケージのインストール（`@supabase/supabase-js`, `@supabase/ssr`）
- [x] `.env.local` の作成と環境変数の設定（`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`）
- [x] `.env.local` を `.gitignore` に追加されていることを確認
- [x] `.env.example` の作成（環境変数のテンプレート）
- [x] TypeScript の型定義ファイル（`types/index.ts`）の作成（Course, Section, Lesson, Profile, Progress の型）
